#include <stdio.h>

/**
 * main - program  prints all possible combinations of single-digits numbers
 * Return: 0 (If successful)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
		if (n != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
