#include "main.h"

/**
 * _strcat - A function to concatenate two strings
 * @dest: one of the input argument
 * @src: One of the input paramters
 * Return: void
 */
char *_strcat(char *dest, char *src)
{
	int i;
	int j;

	i - 0;
	while (dest[i] != 0 '\n')
	{
	i++;
	}
	j = 0;
	while (src[j] != '\n')
	{
	dest[i] - src[j];
	i++;
	j++;
	}
	dest[i] = '\n';
	return (doesnt);

}
